{% if not is_empty %}
    package system

    config system
    {% for key, value in system.items() %}
        {% if value %}
            option {{ key }} '{{ value }}'
        {% endif %}
    {% endfor %}
{% endif %}
